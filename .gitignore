# Рабочие папки — НЕ публикуются (деплой Pages берёт только git-трекаемые файлы).
# Здесь лежат исходники сборки, входные материалы и скриншоты/креативы.
# ВАЖНО: gitignore не поддерживает inline-комментарии — держим их отдельными строками.
_src/
_materials/
_shots/

# Рабочий беклог — не публикуется (внутренние заметки/идеи)
backlog.txt

# Fallback по именам — на случай файлов вне рабочих папок
resume.html
*.src.html
presentation.html
*.pdf
1000000883.jpg
2026-*.png
.DS_Store
